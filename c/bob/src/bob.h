#ifndef BOB_H
#define BOB_H

#include <string.h>
#include <ctype.h>
#include <stdbool.h>

char* hey_bob(const char[]);

bool is_speech(const char[]);
bool is_question(const char[]);
bool is_shouting(const char[]);

#endif
